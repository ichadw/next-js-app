import Typography from "@material-ui/core/Typography";

function Settings(props) {
  return <Typography variant="h6">Settings Page</Typography>;
}

Settings.layout = "private";

export default Settings;
